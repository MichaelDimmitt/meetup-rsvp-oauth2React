{
  "name": "attempt_create_react_app_40k_or_bust",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.16.4",
    "express-static-gzip": "^1.1.3",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-loadable": "^5.5.0",
    "react-scripts": "2.0.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "//": [
      "https://github.com/facebook/create-react-app/issues/1908#issuecomment-295497575",
      "https://github.com/zeit/serve/issues/460#issuecomment-434004280",
      "https://github.com/tkoenig89/express-static-gzip"
    ],
    "ship-to-s3": "rm build/static/*/*.map && ls build/static/* &&  gzip -r build",
    "alternative": "npm run build; npm run ship-to-s3; node server.js"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
